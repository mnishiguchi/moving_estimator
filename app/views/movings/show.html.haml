- provide(:title, @moving.title)

%h1.page-header
  = @moving.title

= link_to "Add new item", { controller: :moving_items, action: :new, moving_id: @moving.id }
.row
  .col-sm-12

    .panel.panel-default
      .panel-heading
        %h3.panel-title
          All items
      .panel-body
        .table-responsive
          %table.table.table-bordered.table-hover.table-striped
            %thead
              %tr
                %th Moving ID
                %th Name
                %th Volume
                %th Quantity
                %th Description
                %th Room
                %th Category
            %tbody
              - @moving_items.each do |mi|
                %tr
                  %td= mi.moving_id
                  %td= mi.name
                  %td= mi.volume
                  %td= mi.quantity
                  %td= mi.description
                  %td= mi.room
                  %td= mi.category
