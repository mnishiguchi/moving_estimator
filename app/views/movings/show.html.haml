- provide(:title, @moving.title)

%h1.page-header
  = @moving.title
  = link_to "Edit", edit_moving_path(@moving), class: "btn btn-primary btn-sm pull-right"

  %small= @moving.description

%ol.breadcrumb
  %li= link_to "My movings", movings_path
  %li.active= @moving.title
%hr/

= link_to "Add new item", new_moving_item_path, class: "btn btn-primary"

= react_component 'MovingRecordsApp', { data: @moving_items }

= "No item found" if @moving_items.count == 0
